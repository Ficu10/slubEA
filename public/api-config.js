// Edit this file before deployment to set the backend API base URL.
// For local development leave it as an empty string (calls go to same origin).
// Example for Cloud Run: window.API_URL = 'https://your-backend-xxxxx.a.run.app';
window.API_URL = (function(){
  // default: same origin
  return '';
})();
